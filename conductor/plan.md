# Plan: Performance Page UX Polish

1. **FilterLinks Navigation**: Refactor `apps/web/src/components/filter-links.tsx`. Update the Band and Model selectors to use the exact same grid/flex layout and CSS styles we just built for the `DashboardSideNav`. This ensures consistency across the site.
name: Nightly Goose Pipeline (disabled - merged into daily_goose_pipeline)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use daily_goose_pipeline.yml"


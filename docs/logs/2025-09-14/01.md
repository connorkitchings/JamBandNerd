# Development Log — 2025-09-14 (Session 01)

- Task Completed: Streamlit app enhancements and pipeline fixes for setlist highlighting and Phish data freshness.

- Key Outcomes:
  - Added Last Show Setlist module beneath the predictions table with model-based highlighting (Top 15 gold, Top 25 silver, Top 50 bronze).
  - Removed predictions table sorting controls to preserve model ordering.
  - Fixed last-show selection logic to choose the most recent show WITH a setlist by intersecting shows and setlists and picking max(show_date).
  - Added date-specific prediction retrieval for highlighting; falls back to latest if no exact-date row exists.
  - Implemented WSP manual setlist insert tool (scripts/admin_add_setlist.py) and inserted 2025-09-12 Richmond show (21 rows; show_id=13988).
  - Updated GitHub Actions daily pipeline to also generate predictions for the last completed show date (per band/model) so highlighting always has correct rows.
  - Repaired Phish setlist ingestion by supplying Postgres-compatible created_at timestamps; re-ingested 2025 setlists; re-ran Phish predictions.

- Blockers Encountered:
  - Goose setlist for 2025-09-12 not available in goose_setlists_raw yet (API lag); app correctly shows 2025-08-31 as latest with a setlist.
  - Supabase upsert initially failed for phish_setlists_raw due to NOT NULL created_at; resolved by normalizing created_at format.

- Session Handoff & Next Steps:
  - If we want the app to always show the latest show date (even when the setlist is not yet available), add a "Setlist pending" path and still use date-specific predictions for that date.
  - If needed, add a temporary debug panel in the Streamlit app to print: reference_date, recently_played_songs count/sample, and intersection with Top-50 predictions for rapid diagnostics.
  - Monitor daily workflow to ensure last completed show predictions are generated successfully for all bands; confirm with Supabase rows.
  - When Goose 2025-09-12 setlist is published, verify the app switches from 8/31 to 9/12 automatically and highlights correctly.

- Updated Documents:
  - docs/developer_guide/ai_sessions.md (read & followed; no edits required)
  - docs/logs/2025-09-14/01.md (this log)
  - .github/workflows/daily-pipeline.yml (generate predictions for last completed show)

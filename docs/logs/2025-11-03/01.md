# Dev Log - 2025-11-03

## Session 1

- **Task Completed**: Improve performance and efficiency of the optimized pipelines for WSP.
- **Key Outcomes**:
  - Identified duplicate setlist entries for WSP shows.
  - Attempted to fix the duplicate entries by modifying the collection script, but was unsuccessful.
  - Attempted to modularize the WSP collector, but was unsuccessful.
- **Blockers Encountered**:
  - Indentation errors in Python code.
  - `nonlocal` variable scoping issues.
  - `TypeError` due to using `|` for type hinting in an older Python version.
  - `NameError` due to missing import.
- **Session Handoff & Next Steps**:
  - The duplicate setlist issue for WSP is still unresolved. The next session should focus on debugging the `parse_setlist_from_text` function in `src/jambandnerd/data_collection/wsp/parser.py` to correctly handle the setlist data.
  - The modularization of the WSP collector should be revisited.
- **Updated Documents**:
  - `docs/logs/2025-11-03/01.md` (new file)

---

## Session 2

- **Task Completed**: Refactor and fix the Widespread Panic (WSP) data collection method to be modular, prevent duplicate data, and improve efficiency.
- **Key Outcomes**:
  - Refactored the WSP collection logic into a modular architecture, with the core logic in `src/jambandnerd/data_collection/wsp/orchestration.py`.
  - Implemented logic to prevent duplicate setlist scraping by checking the database before making network requests.
  - Resolved multiple runtime errors, including `TypeError` for non-serializable data and `NameError` due to incorrect variable names.
  - Optimized the script to fetch only the shows for the specified year range, significantly improving performance.
  - Successfully ran the end-to-end WSP collection script for 2025, which now runs in seconds instead of minutes.
- **Blockers Encountered**:
  - `TypeError: fetch_table() got an unexpected keyword argument 'gte'`: This was due to an incorrect assumption about the `fetch_table` function's capabilities.
  - `NameError: name 'shows_to_process' is not defined`: This was due to a copy-paste error after refactoring.
- **Session Handoff & Next Steps**:
  - The WSP data collection pipeline is now modular, efficient, and robust. The next session can focus on other bands or further enhancements.
- **Updated Documents**:
  - `src/jambandnerd/data_collection/wsp/orchestration.py`
  - `scripts/run_wsp_collection.py`
  - `src/jambandnerd/data_collection/wsp/collector.py`
  - `docs/logs/2025-11-03/01.md`
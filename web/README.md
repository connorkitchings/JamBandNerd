# Legacy Web Placeholder

The maintained website lives in `apps/web`.

This top-level `web/` directory is not a runtime application. It is retained as
a minimal compatibility placeholder for repository checks that still expect the
path to exist.
